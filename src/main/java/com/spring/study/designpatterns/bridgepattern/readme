桥接模式：
    定义：将实现与抽象放在两个不同的层次中，使两个层次可以独立改变，用组合关系代替继承关系，降低了抽象和实现两个可变维度的耦合度
    结构：
        抽象化：定义抽象类，包含对实现化对象的引用
        扩展抽象化：抽象化角色的子类，实现父类中的业务方法，并通过组合关系调用实现化角色的方法
        实现化：定义实现化接口，供抽象化角色调用
        具体实现化：实现化接口的具体实现
    优点：
        提高扩展性，两个维度中任意扩展一个维度，都不需要改原系统。
    适用场景：
        当一个类存在两个变化的维度，且两个维度都需要扩展时
        当系统不希望通过继承或者因多层继承导致系统类剧增时

