享元模式：
    定义：运用共享技术，来支持大量细粒度对象的复用，通过共享已经存在的对象来减少需要创建的对象，避免大量相似对象的开销，提供资源利用率
    两种状态：
        内部状态：不会随着环境改变而改变的可共享部分
        外部状态：会改变的不可共享部分
    结构：
        抽象享元角色：接口或者抽象类，声明了享元类的共享方法，
        具体享元角色：继承、实现抽象享元角色，
        非享元角色：
        享元工厂角色：
    优点：
        减少相似对象的数量，节省资源
        外部状态相对独立且不影响内部状态
    缺点：
        分离内部状态和外部状态，逻辑复杂
    适用场景：
        一个系统中有大量相似对象
        对象的大部分状态可以外部化，可以将这些外部状态传入对象中
        需要维护一个存储享元对象的享元池，应当在需要多次重复使用享元对象时才值得使用享元模式
    JDK中的使用：
        Integer类中使用了该模式，缓存了-128~127的Integer对象作为享元池

