策略模式：
    定义：定义了一系列算法，并将每个算法封装起来，把算法的使用和实现分割，并委派给不同的对象对这些算法进行管理
    角色：
        抽象策略：给出所有的算法，通常是接口或者抽象类
        具体策略：实现抽象策略，实现具体的算法
        环境：持有一个策略类的引用，给客户端使用
    优点：
        策略类之间可以自由切换
        易扩展，增加一个策略只需要增加一个策略实现类即可，符合开闭原则
    缺点：
        客户端需要知道所有的策略类，自行决定使用哪个策略
    适用场景：
        需要动态的在多个算法之间选择一种时，可以将算法封装到策略类中