组合（部分整体）模式：
    定义：把一组相似的对象当做一个单一的对象，依据树形结构来组合对象，用来表示部分及整体整体层次
    结构：
        抽象根节点：定义系统各层次对象共有的方法和属性，可预先定义一些行为和属性
        树枝节点：定义树枝节点的行为，存储子节点，组合树枝节点和叶子节点形成一个树形结构
        叶子节点：叶子节点对象，是层次遍历的最小单位
    分类：
        透明组合模式：
        安全组合模式：
    适用场景：
        需要遍历组织结构，或者处理的对象具有树形结构
        具有较高的抽象性，如果根节点、叶子节点差异大的话，则不适用