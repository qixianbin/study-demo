模板方法模式：
    定义：定义了操作中的算法骨架， 将算法的一些步骤推迟到子类中，使得子类在不改变算法结构的情况下重新定义某些算法的实现。
    结构：
        抽象类：给出算法的骨架，由一个模板方法和若干基本方法组成，模板方法一般定义为final，防止子类修改
        具体子类：实现抽象类中的抽象方法
    优点：
        相同算法方法放在父类中，提高代码复用性
        实现反向控制，父类的算法骨架调用子类的具体方法，符合开闭原则
    缺点：
        每个不同的实现都需要创建一个子类，类会过多，系统更加庞大
        父类调用子类的具体方法，代码阅读难度增加
    适用场景：
        算法步骤固定，个别算法会变化
    JDK源码使用：
        InputStream中定义了read（三个参数）的模板方法调用了抽象的read（无参）基本方法，无参的read由子类实现