命令模式：
    定义：将一个请求封装成一个对象，使发出请求的责任和执行请求的责任分开，两者之间通过命令对象沟通，方便将命令对象进行存储、传递、调用、增加、管理。
    结构：
        抽象命令类：定义命令接口
        具体命令类：实现命令接口，通常持有接受者，并调用接受者执行命令的方法
        接收者：执行命令的对象，任何类都可以是接受者，只要能执行命令即可
        调用者：要求命令对象执行请求，通常会持有一个或多个命令对象，这是客户端真正触发命令的地方
    优点：
        降低调用者、接受者耦合度
        增加命令方便，不会影响其他类，满足开闭原则
        可以与组合模式结合使用，实现宏命令
        可以与备忘录模式结合，实现命令的撤销与恢复
    缺点：
        系统结构复杂
        可能会导致命令类过多
    适用场景：
        需要将请求调用者和接受者解耦
    JDK源码：
